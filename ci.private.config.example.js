// 复制本文件为 ci.private.config.js 并填写真实值（该文件已被 .gitignore 忽略，不会被提交）
module.exports = {
  // 小程序 AppID（mp.weixin.qq.com → 开发 → 开发管理 → 开发设置）
  appid: 'wx1234567890abcdef',
  // 上传密钥文件路径（小程序后台 → 开发设置 → 小程序代码上传 → 下载上传密钥）
  privateKeyPath: './private.key',
  // 云开发环境 ID（用于提示，见 miniprogram/config.js）
  cloudEnv: 'your-cloud-env-id'
}
